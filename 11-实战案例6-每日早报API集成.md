# 📰 实战案例 6：集成「每日早报」API

> **目标**：在天气 App 中增加「每日早报」模块，调用 ALAPI `/api/zaobao` 接口，每天 60 秒读懂世界  
> **前置条件**：已完成实战案例 4-5，理解 Tauri 命令的请求/响应模式

---

## 一、场景设计

点击加载当天的全球新闻速报（15 条），以及每日微语。

```
┌─────────────────────────────┐
│  📰 每日早报  (点击加载)      │
│                             │
│  2026-06-21                 │
│                             │
│  1. 中央气象台：冷空气将...   │
│  2. 商务部：拟限制磷酸铁锂... │
│  3. 香港往返西安高铁...      │
│  4. ...                      │
│  ...                         │
│                             │
│  ┌─────────────────────────┐ │
│  │  【微语】                │ │
│  │  人们总说时间可以改变     │ │
│  │  很多事，但事实上必须由    │ │
│  │  你自己做出那些改变。      │ │
│  └─────────────────────────┘ │
└─────────────────────────────┘
```

---

## 二、ALAPI 接口分析

### 2.1 接口信息

| 项目 | 内容 |
|------|------|
| ALAPI ID | 67 |
| 端点 | `POST https://v3.alapi.cn/api/zaobao` |
| 请求方法 | POST (JSON body) |
| 必填参数 | `token` |
| 可选参数 | `format` (返回格式，默认 `json`) |

### 2.2 响应结构

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "date": "2025-01-06",
    "news": [
      "1、中央气象台：冷空气将席卷中东部...",
      "2、商务部：拟限制磷酸铁锂..."
    ],
    "weiyu": "【微语】人们总说时间可以改变很多事...",
    "image": "",
    "head_image": ""
  }
}
```

---

## 三、Rust 后端实现

```rust
/// 每日早报 — POST /api/zaobao
#[tauri::command]
async fn morning(token: String) -> Result<String, String> {
    let client = reqwest::Client::new();
    let mut params = HashMap::new();
    params.insert("token", token.as_str());
    let resp = client
        .post("https://v3.alapi.cn/api/zaobao")
        .json(&params)
        .send()
        .await
        .map_err(|e| format!("请求失败: {e}"))?;
    let text = resp.text().await.map_err(|e| format!("读取响应失败: {e}"))?;
    Ok(text)
}
```

**注意**：该接口的响应字段是 `code`/`msg` 而非 `success`/`message`，前端解析时需要兼容处理。

### 注册命令

```rust
.invoke_handler(tauri::generate_handler![
    greet,
    get_weather,
    today,
    constellation,
    morning,  // ← 新增
])
```

---

## 四、Vue 前端实现

### 4.1 响应式数据

```typescript
// ==================== 每日早报 ====================
const morningData = ref<any>(null);
const morningLoading = ref(false);
const morningError = ref("");

async function fetchMorning() {
  if (morningData.value) return; // 已缓存
  morningLoading.value = true;
  morningError.value = "";
  try {
    const res = await invoke<string>("morning", {
      token: token.value,
    });
    const parsed = JSON.parse(res);
    // 兼容不同响应格式（success/message vs code/msg）
    if (parsed.success || parsed.code === 200) {
      morningData.value = parsed.data;
    } else {
      morningError.value = parsed.message || "获取失败";
    }
  } catch (e) {
    morningError.value = `请求出错: ${e}`;
  } finally {
    morningLoading.value = false;
  }
}
```

### 4.2 模板

```html
<section class="card feature-card">
  <h3 class="feature-header" @click="fetchMorning">
    📰 每日早报
    <span class="toggle-hint">{{ morningData ? '' : '点击加载' }}</span>
  </h3>
  <div v-if="morningLoading" class="feature-loading">
    <span class="spinner"></span> 加载中...
  </div>
  <div v-else-if="morningError" class="error-msg">{{ morningError }}</div>
  <div v-else-if="morningData" class="morning-content">
    <div class="morning-date">{{ morningData.date }}</div>
    <div v-for="(news, i) in morningData.news" :key="i" class="morning-news-item">
      <span class="morning-index">{{ i + 1 }}</span>
      <span>{{ news }}</span>
    </div>
    <div v-if="morningData.weiyu" class="morning-weiyu">
      {{ morningData.weiyu }}
    </div>
  </div>
</section>
```

### 4.3 样式

```css
.morning-content {
  max-height: 520px;
  overflow-y: auto;
}
.morning-date {
  font-size: 0.85em;
  color: #999;
  margin-bottom: 10px;
  text-align: center;
}
.morning-news-item {
  display: flex;
  gap: 8px;
  padding: 6px 0;
  font-size: 0.88em;
  line-height: 1.6;
  border-bottom: 1px solid #f0f0f0;
}
.morning-index {
  font-weight: 700;
  color: #396cd8;
  min-width: 22px;
  text-align: right;
  flex-shrink: 0;
}
.morning-weiyu {
  margin-top: 12px;
  padding: 12px;
  background: #fef8e8;
  border-radius: 10px;
  font-size: 0.88em;
  color: #b8860b;
  line-height: 1.6;
  font-style: italic;
}
```

---

## 五、编译验证

```bash
cd src-tauri && cargo check
```

---

## 六、鸿蒙真机验证

```bash
cd src-tauri && cargo tauri ohos build
hdc install gen/ohos/entry/build/default/outputs/default/entry-default-signed.hap
hdc shell aa start -a EntryAbility -b com.ranger.tauri_demo
```

打开 App → 点击「📰 每日早报」→ 15 条新闻速报和微语展示。

---

## 七、小结

| 知识点 | 内容 |
|--------|------|
| ALAPI 接口 | 每日早报 (`/api/zaobao`) |
| 响应格式差异 | `code`/`msg` vs `success`/`message`，前端做兼容 |
| 数据缓存 | 同一策略：`if (data.value) return` 避免重复请求 |
| 微语样式 | 暖色背景 + 斜体，与新闻列表区分 |
