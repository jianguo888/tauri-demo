# 📜 实战案例 4：集成「历史上的今天」API

> **目标**：在天气 App 中增加「历史上的今天」模块，调用 ALAPI `/api/eventHistory` 接口，在鸿蒙手机上查看历史上的今天发生了什么  
> **前置条件**：已完成前 3 个实战案例，能成功编译和运行项目

---

## 一、场景设计

在天气 App 底部增加一个「历史上的今天」卡片，点击加载当天发生的历史事件。

```
┌─────────────────────────────┐
│  📜 历史上的今天  (点击加载)  │
│                             │
│  1047年  鲁国公主出生        │
│          中国历史上最长寿的    │
│          公主庆寿公主出生...  │
│                             │
│  1933年  察哈尔民众抗日同盟军 │
│          开赴前线抗日        │
│           ...               │
│                             │
│  1870年  天津教案发生        │
└─────────────────────────────┘
```

---

## 二、ALAPI 接口分析

### 2.1 接口信息

| 项目 | 内容 |
|------|------|
| ALAPI ID | 11 |
| 端点 | `POST https://v3.alapi.cn/api/eventHistory` |
| 请求方法 | POST (JSON body) |
| 必填参数 | `token` |
| 可选参数 | `date` (格式 `MMDD` 或 `MM-DD`，默认当天) |

### 2.2 响应结构

```json
{
  "success": true,
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "3880cad353ba11eb90470c42a1415493",
      "title": "意大利同意部署美国导弹",
      "year": 1983,
      "month": 11,
      "day": 26,
      "monthday": "1126",
      "date": "1983年11月26日",
      "desc": "..."
    }
  ]
}
```

---

## 三、Rust 后端实现

在 `src-tauri/src/lib.rs` 中新增 Tauri 命令：

```rust
/// 历史上的今天 — POST /api/eventHistory
#[tauri::command]
async fn today(token: String, date: Option<String>) -> Result<String, String> {
    let client = reqwest::Client::new();
    let mut params = HashMap::new();
    params.insert("token", token.as_str());
    if let Some(ref d) = date {
        params.insert("date", d.as_str());
    }
    let resp = client
        .post("https://v3.alapi.cn/api/eventHistory")
        .json(&params)
        .send()
        .await
        .map_err(|e| format!("请求失败: {e}"))?;
    let text = resp.text().await.map_err(|e| format!("读取响应失败: {e}"))?;
    Ok(text)
}
```

**要点说明**：
- `date` 为 `Option<String>`，不传则默认当天
- 复用已有的 `reqwest::Client` 和 token
- 返回原始 JSON 字符串，由前端解析

### 注册命令

在 `generate_handler!` 宏中追加：

```rust
.invoke_handler(tauri::generate_handler![
    greet,
    get_weather,
    today,          // ← 新增
    // ...
])
```

---

## 四、Vue 前端实现

### 4.1 响应式数据与请求函数

```typescript
// ==================== 历史上的今天 ====================
const todayData = ref<any>(null);
const todayLoading = ref(false);
const todayError = ref("");

async function fetchTodayHistory() {
  if (todayData.value) return; // 已缓存，避免重复请求
  todayLoading.value = true;
  todayError.value = "";
  try {
    const res = await invoke<string>("today", {
      token: token.value,
      date: null,
    });
    const parsed = JSON.parse(res);
    if (parsed.success && parsed.data) {
      todayData.value = parsed.data;
    } else {
      todayError.value = parsed.message || "获取失败";
    }
  } catch (e) {
    todayError.value = `请求出错: ${e}`;
  } finally {
    todayLoading.value = false;
  }
}
```

### 4.2 模板

```html
<section class="card feature-card">
  <h3 class="feature-header" @click="fetchTodayHistory">
    📜 历史上的今天
    <span class="toggle-hint">{{ todayData ? '' : '点击加载' }}</span>
  </h3>
  <div v-if="todayLoading" class="feature-loading">
    <span class="spinner"></span> 加载中...
  </div>
  <div v-else-if="todayError" class="error-msg">{{ todayError }}</div>
  <div v-else-if="todayData" class="today-list">
    <div v-for="evt in todayData" :key="evt.id" class="today-item">
      <span class="today-year">{{ evt.year }}年</span>
      <div class="today-body">
        <strong>{{ evt.title }}</strong>
        <p v-if="evt.desc">{{ evt.desc }}</p>
      </div>
    </div>
  </div>
</section>
```

### 4.3 样式

```css
.today-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
  max-height: 400px;
  overflow-y: auto;
}
.today-item {
  display: flex;
  gap: 10px;
  align-items: flex-start;
}
.today-year {
  font-size: 0.78em;
  font-weight: 700;
  color: #396cd8;
  white-space: nowrap;
  min-width: 48px;
}
.today-body strong {
  font-size: 0.92em;
}
.today-body p {
  margin: 2px 0 0;
  font-size: 0.82em;
  color: #888;
  line-height: 1.5;
}
```

---

## 五、编译验证

```bash
cd src-tauri && cargo check
```

输出示例：

```
Compiling tauri-demo v0.1.0
Finished `dev` profile [unoptimized + debuginfo]
```

---

## 六、鸿蒙真机验证

```bash
cd src-tauri && cargo tauri ohos build
hdc install gen/ohos/entry/build/default/outputs/default/entry-default-signed.hap
hdc shell aa start -a EntryAbility -b com.ranger.tauri_demo
```

打开 App → 点击「📜 历史上的今天」→ 列表展开，显示当天历史事件。

---

## 七、小结

| 知识点 | 内容 |
|--------|------|
| ALAPI 接口 | 历史上的今天 (`/api/eventHistory`) |
| Rust 命令设计 | `Option<String>` 处理可选日期参数 |
| 前端缓存 | `if (todayData.value) return` 防重复请求 |
| 响应式适配 | 桌面端 2 列网格，手机端单列列表 |
